language: go

go:
  - 1.8.x
  - master

install:
  - go get github.com/mattn/goveralls
  - go get github.com/Masterminds/glide
  - glide i

script:
  - go build github.com/flowup/mmo
  - $HOME/gopath/bin/goveralls -service=travis-ci